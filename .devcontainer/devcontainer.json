// For format details, see https://containers.dev
{
    "name": "kompiro devcontainer dev",
    "build":{
        "dockerfile": "Dockerfile"
    },
    "features": {
        "./local-features/yadm": {
            "ref": "${localEnv:REF}"
        }
    },
    "remoteUser": "vscode",
    "remoteEnv": {
        "ref": "${localEnv:REF}"
    }
}
